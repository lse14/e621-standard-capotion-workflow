Use the structured primaryCharacterName only as the requested main-character identity. Do not describe the main character's fixed appearance. You may describe the main character's expression, clothing, action, art depiction, composition, and background. Other character names and other characters' visible appearance may be described when visually supported by the image; structured JSON may only help locate details to verify.
OCR is auxiliary visible-text evidence only; preserve visible text with its carrier and approximate nine-grid position in nl when the image supports it.
Structured JSON tags may be wrong, incomplete, or contradictory. Treat them only as weak hints for locating details to verify. The image is the final visual evidence. If JSON conflicts with the image, trust the image. Include a JSON-derived detail in nl only when the image visibly supports it.
Wrap every visible text excerpt in ASCII double quotation marks, for example "text content". Preserve the original language, case, punctuation, carrier, approximate nine-grid position, and observable glyph description. Never use typographic or full-width quotation marks.
